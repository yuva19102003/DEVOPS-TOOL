docker run --rm \
  --network=host \
  -e SONAR_HOST_URL="http://localhost:9000" \
  -e SONAR_TOKEN="YOUR_SONAR_TOKEN" \
  -v "$(pwd):/usr/src" \
  sonarsource/sonar-scanner-cli -X




docker run --rm \
  --network=host \
  -e SONAR_HOST_URL="http://localhost:9000" \
  -e SONAR_TOKEN="qp_ca582011b74934d60cb73eedf0a1e889cc9dc8e1" \
  -v "$(pwd):/usr/src" \
  sonarsource/sonar-scanner-cli -X
